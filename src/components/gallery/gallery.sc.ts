import styled from 'styled-components'

export const GalleryWrapper = styled.div`
  width: 34.25rem;
  height: 34.25rem;
  z-index: 0;

  & img {
    display: inline;
    margin: 0 auto;
    height: 100%;
    transform: scale(1.2);
  }
`;