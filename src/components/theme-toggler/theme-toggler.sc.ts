import styled from "styled-components";

export const ModeTrigger = styled.button`
  border: none;
  background: none;
  color: ${({ theme }) => theme.color};
  font-size: 2.5rem;
  padding: 0;
  font-family: Bigilla;
`;
